// import ScheduleState from '../../model/state/ScheduleState'
// import { ScheduleDto } from '../../model/dto/ScheduleDto';

// const map = (dto: ScheduleDto, state: ScheduleState): ScheduleState => ({
//     id: dto.id,
//     name: dto.name,
//     batchSize: dto.batch_size,
//     batchInterval: dto.batch_interval,
//     // sleepTime: dto.time_between_batches,
//     // startSending: dto.start_at
// })

// export default map;