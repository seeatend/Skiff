interface ScheduleDto {
    "start_type": string,
    "start_at": null,
    "name": string,
    "batch_interval": number,
    "time_between_batches": number,
    "id": number,
    "batch_size": number
}

export default ScheduleDto;