import { ValidationResponseDto } from '../../../model/dto/ValidationResponseDto';
import { LoginState } from '../../../model/state/LoginState';

export const mapLogin = (dto: ValidationResponseDto): LoginState => {
    return
}