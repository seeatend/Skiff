import { ValidationResponseDto } from '../../../model/dto/ValidationResponseDto';
import { UserEditState, UserAddState } from '../../../model/state/UserState';

export const mapUser = (dto: ValidationResponseDto): UserEditState | UserAddState => {
    return
}