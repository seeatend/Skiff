{
  "name": "Skiff",
  "version": "0.1.0",
  "description": "Proprietary Front-End Application for use with Rhino Security Labs Sandbar project",
  "author": "Michael Payne @ Rhino Security Labs",
  "license": "UNLICENSED",
  "private": true,
  "homepage": "http://rhinosecuritylabs.com",
  "config": {
    "env": "local"
  },
  "scripts": {
    "clean": "rm -rf build && rm -rf dist",
    "install:latest": "npm install --save-dev typings@* typescript@* istanbul@* jasmine@*",
    "init": "rm -rf .git && npm run install:latest && npm init && git init",
    "compile": "tsc",
    "build:tsc": "npm run compile",
    "prebuild": "npm run clean && mkdir -p dist",
    "build": "npm run build:tsc && npm run bundle",
    "build:dev": "npm run build --skiff:env=dev",
    "build:prod": "npm run build --skiff:env=prod",
    "postbuild": "cp -r assets dist/ && mkdir dist/assets/js && cp build/lib/bundle.js dist/assets/js/skiff-$npm_package_version.js && cp index.html dist/",
    "setup:mock": "cd mock && npm run teardown:mock && docker-compose up -d",
    "teardown:mock": "cd mock && docker-compose down",
    "prebundle": "mkdir build/lib",
    "bundle": "browserify --im build/js/main.js > build/lib/bundle.js",
    "prepack": "",
    "test": "rm -rf build && npm run build:tsc && istanbul cover jasmine",
    "posttest": "istanbul check-coverage --statements 80 --functions 80 --branches 80 --lines 80",
    "publish:major": "npm version major && npm run prepack && npm publish build",
    "publish:minor": "npm version minor && npm run prepack && npm publish build",
    "publish:patch": "npm version patch && npm run prepack && npm publish build",
    "typings": "typings",
    "start": "php -S localhost:8000 -t dist/ local_dev/router.php"
  },
  "dependencies": {
    "dotenv": "^2.0.0",
    "jwt-decode": "^2.1.0",
    "moment": "^2.15.1",
    "popsicle": "^8.2.0",
    "react": "^15.3.2",
    "react-addons-css-transition-group": "^15.3.2",
    "react-dom": "^15.3.2",
    "react-redux": "^4.4.5",
    "react-router": "^2.8.1",
    "redux": "^3.6.0"
  },
  "devDependencies": {
    "browserify": "^13.1.0",
    "istanbul": "^0.4.5",
    "jasmine": "^2.5.2",
    "npm-run-all": "^3.1.0",
    "typescript": "^2.0.3",
    "typings": "^1.4.0"
  },
  "main": "index.js",
}
